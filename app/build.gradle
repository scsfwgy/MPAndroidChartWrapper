apply plugin: 'com.android.application'
apply from: rootProject.file('lib_common.gradle')

android {
    defaultConfig {
        applicationId "com.matt.demo"
        versionCode 1
        versionName "1.0"
    }
}

dependencies {
    api project(path: ':sample_binance')
}
